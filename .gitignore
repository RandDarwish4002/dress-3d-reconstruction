# Environments
triposr_env/
venv/
__pycache__/
*.pyc
.pytest_cache/

# TripoSR runtime source (cloned by setup_env.sh, not our code)
triposr/

# Data: user photos and generated meshes are large / private, don't commit them
input/*
!input/.gitkeep
output/*
!output/.gitkeep

# Secrets — NEVER commit tokens or credentials
*.token
.env
*.pem
secrets.json

# OS / IDE
.DS_Store
.idea/
.vscode/
